'use client'
import { useEffect,useState } from 'react';import { Search as SearchIcon } from 'lucide-react';import { MediaCard,Shell } from '@/components/luma';import { searchMulti,Media } from '@/lib/tmdb'
export default function Search(){const [q,setQ]=useState('');const [items,setItems]=useState<Media[]>([]);const [loading,setLoading]=useState(false);useEffect(()=>{if(!q.trim()){setItems([]);return}const t=setTimeout(async()=>{setLoading(true);try{setItems((await searchMulti(q)).results.filter(x=>x.media_type==='movie'||x.media_type==='tv'))}catch{setItems([])}finally{setLoading(false)}},450);return()=>clearTimeout(t)},[q]);return <Shell><div className="px-5 pt-14 lg:px-8"><p className="eyebrow">Find your next signal</p><h1 className="mt-3 text-5xl font-black tracking-[-.06em] text-white">Search</h1><label className="mt-8 flex max-w-2xl items-center gap-3 rounded-2xl border border-white/10 bg-white/5 px-4 focus-within:border-primary"><SearchIcon className="text-muted-foreground" size={20}/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search movies and series" className="h-14 flex-1 bg-transparent text-white outline-none placeholder:text-muted-foreground"/></label></div><div className="mt-10 grid grid-cols-2 gap-4 px-5 sm:grid-cols-4 lg:grid-cols-6 lg:px-8">{items.map((item,i)=><MediaCard key={`${item.id}-${i}`} item={item}/>)}</div><div className="p-8 text-center text-sm text-muted-foreground">{loading?'Searching…':q&&!items.length?'No titles found.':!q?'Start with a title, genre, or mood.':''}</div></Shell>}
