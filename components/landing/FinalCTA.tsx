import Image from 'next/image'
import Link from 'next/link'
import { backdrop, titleOf, type Media } from '@/lib/tmdb'
import { mediaHref } from './landing-types'

export function FinalCTA({ item }: { item?: Media }) {
  return <section className="relative isolate overflow-hidden px-5 py-28 sm:px-6 lg:px-12" aria-labelledby="final-cta-heading"><div className="absolute inset-0 -z-20 bg-[#0b111a]" />{item?.backdrop_path ? <Image src={backdrop(item.backdrop_path, 'w1280')} alt="" fill sizes="100vw" className="-z-10 object-cover opacity-25" /> : null}<div className="absolute inset-0 -z-10 bg-gradient-to-r from-[#050507] via-[#050507]/90 to-[#050507]/70" /><div className="mx-auto max-w-3xl text-center"><p className="eyebrow">The signal is open</p><h2 id="final-cta-heading" className="mt-3 font-display text-4xl font-bold tracking-tight text-white sm:text-6xl">The next signal is already playing.</h2><p className="mx-auto mt-5 max-w-2xl text-base leading-7 text-white/70">Discover the movie or series worth your time, then keep it close in your own library.</p><div className="mt-9 flex flex-wrap justify-center gap-3"><Link href="/browse" className="inline-flex min-h-11 items-center rounded-full bg-[#b8f7d4] px-6 text-sm font-bold text-[#050507] hover:bg-[#d2ffe7] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#b8f7d4]">Explore VEYRA</Link>{item ? <Link href={mediaHref(item)} className="inline-flex min-h-11 items-center rounded-full border border-white/25 px-6 text-sm font-semibold text-white hover:border-white/60 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#b8f7d4]">Open trending: {titleOf(item)}</Link> : null}</div></div></section>
}
