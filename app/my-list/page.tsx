'use client'
import { useEffect,useState } from 'react';import { Empty, MediaCard, Shell } from '@/components/luma';import type { Media } from '@/lib/tmdb'
export default function MyList(){const [items,setItems]=useState<Media[]>([]);useEffect(()=>setItems(JSON.parse(localStorage.getItem('luma-watchlist')||'[]')),[]);return <Shell><div className="px-5 pt-14 lg:px-8"><p className="eyebrow">Your shelf</p><h1 className="mt-3 text-5xl font-black tracking-[-.06em] text-white">My list</h1></div><div className="mt-10 grid grid-cols-2 gap-4 px-5 sm:grid-cols-4 lg:grid-cols-6 lg:px-8">{items.map((item,i)=><MediaCard key={`${item.id}-${i}`} item={item}/>)}</div>{!items.length&&<div className="mx-5 mt-10 lg:mx-8"><Empty title="Your list is quiet" text="Save a movie or series and it will appear here." action={<a href="/movies" className="rounded-full bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Explore movies</a>}/></div>}</Shell>}
