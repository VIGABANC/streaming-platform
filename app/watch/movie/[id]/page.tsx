import Link from 'next/link';import { ArrowLeft } from 'lucide-react';import { getDetail,titleOf } from '@/lib/tmdb';import { Player,Shell } from '@/components/luma';import { getMovieEmbedUrl } from '@/lib/player';export default async function Watch({params}:{params:Promise<{id:string}>}){const {id}=await params;const item=await getDetail('movie',id).catch(()=>null);return <Shell><div className="p-5 lg:p-10"><Link href={`/movie/${id}`} className="mb-5 inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-white"><ArrowLeft size={16}/>Back to details</Link><Player src={getMovieEmbedUrl(id)} title={`${item?titleOf(item):'Movie'} playback`} artwork={item?.backdrop_path ? `https://image.tmdb.org/t/p/w1280${item.backdrop_path}` : undefined}/><h1 className="mt-6 text-2xl font-bold text-white">{item?titleOf(item):'Movie'}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">{item?.overview}</p></div></Shell>}
